* Generic

- Tag PKs and FKs
- Subselects, Exists, not exists
- SQL insert
- SQL update
- SQL delete
- Unions
- select *
- FOR UPDATE, LOCK MODE, etc.
- in operator
- Arithmetic (update player set age=age+1)
- Make parser case insensitive (keywords only)
- Infer function types
- A mechanism to tranform or combine values

* MySQL

- ON DUPLICATE KEY
